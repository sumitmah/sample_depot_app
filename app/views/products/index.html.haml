%h1 Products

.products
  %table
    - @products.each do |product|
      %tr[product]

        %td= image_tag(product.image_url, class: 'list_image')
        %td.list_description
          %dl
            %dt= product.title
            %dd= truncate(strip_tags(product.description), length: 80)

        %td.list_actions
          #{ link_to 'View', product_path(product)}
          #{ link_to 'Edit', edit_product_path(product.id)}
          #{ link_to 'Destroy', {action:'destroy', controller:'products', id: product.id}, method: 'delete'}
  %br
    Add #{ link_to 'New Product', '/products/new'}
